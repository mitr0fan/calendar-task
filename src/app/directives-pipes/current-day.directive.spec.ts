import { CurrentDayDirective } from './current-day.directive';

describe('CurrentDayDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentDayDirective();
    expect(directive).toBeTruthy();
  });
});
